interface:
  display_name: "Dental Evidence Retriever"
  short_description: "Reproducible dental literature searches"
  brand_color: "#0F766E"
  default_prompt: "Use $dental-evidence-retriever to convert this dental question into PICO, source-specific search strategies, and a retrieval log."
policy:
  allow_implicit_invocation: true
